enable_testing()
add_executable(day-{{day}} main.cpp)
target_link_libraries(day-{{day}} GTest::gtest Common)

file(GLOB SOURCES "src/*.cpp")
target_sources(day-{{day}} PRIVATE ${SOURCES})
target_include_directories(day-{{day}} PRIVATE "src")