/**
 * Day-18 Include Header
 */
#pragma once

#include <bits/stdc++.h>
#include "Utils.hpp"

int64_t handlePart1(const std::vector<std::string> &inputLines, uint32_t grid_size = 71, uint32_t bytes = 1024);
int64_t handlePart2(const std::vector<std::string> &inputLines);